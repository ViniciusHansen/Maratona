#include <bits/stdc++.h>

using namespace std;

set<char> setpy(string s){
    set<char> st;
    for(int i=0;i<s.size();i++)
        st.insert(s[i]);
    return st;
}
//desconsiderar o *
int main(){
    int n,c;
    vector<string> words;
    cin >> n >> c;
    for(int i =0;i<n;i++){
        string temp;
        cin >> temp;
        words.push_back();
    }
    map<char,int> o;
    for(String word : words){
        o.insert()
    }
        ocorrencias[setpy(word)]++;
    //pegar a chave com maior ocorrencia







    return 0;
}
